info:
  name: "Login "
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3001/auth/login
  body:
    type: json
    data: |-
      {
        "email": "devlogin2@example.com",
        "password": "dev123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
